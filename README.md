# 깃헙 주간 트렌드 리포트

codex 로 리포트 생성하기

```bash
codex -a never --sandbox danger-full-access "$(cat weekly-trend-report-prompt.md)"
```

> 작업 실행 과정에서 네트워크 접근 및 파일 저장이 필요. interaction 없이 수행하기 위해 approval / sandbox 옵션을 full access 로 조정하여 실행함.
